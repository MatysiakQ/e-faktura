package com.example.e_faktura.ui.theme

import androidx.compose.ui.graphics.Color

// Primary Palette (Navy)
val NavyPrimary = Color(0xFF0A192F)       // Główny, ciemny granat
val NavyLight = Color(0xFF1E3A5F)        // Jaśniejszy odcień do teł i kontenerów
val NavyDark = Color(0xFF051120)         // Ciemniejszy do akcentów w trybie ciemnym

// Accent Palette (Lime Green)
val LimeAccent = Color(0xFF32CD32)         // Energetyczny, limonkowy
val LimeLight = Color(0xFF98FB98)         // Jaśniejszy dla subtelnych akcentów

// Neutral Palette
val GreyLight = Color(0xFFF0F4F8)         // Bardzo jasny szary do teł (tryb jasny)
val GreyMedium = Color(0xFFBCCCDC)       // Szary do obramowań i tekstu pomocniczego
val GreyDark = Color(0xFF8A9AAF)         // Ciemniejszy szary do mniej ważnych tekstów
val White = Color(0xFFFFFFFF)
val Black = Color(0xFF000000)

// Semantic Colors
val SuccessGreen = Color(0xFF28A745)       // Kolor sukcesu (np. opłacona faktura)
val ErrorRed = Color(0xFFDC3545)         // Kolor błędu (np. nieopłacona faktura)
val WarningOrange = Color(0xFFFFC107)     // Kolor ostrzeżenia